import { Routes } from '@angular/router';
import { GridComponent } from './modules/tipoveiculo/grid/grid.component';

export const routes: Routes = [
    { path: 'grid-tipoveiculo', component: GridComponent }
];
